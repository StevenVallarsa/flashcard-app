extends layout.pug

block content
  section#content
    ul 
      each color in colors
        li(style=`color:${color}`) #{color}
    h2= prompt
    if hint
      p Hint: #{hint}
    else 
      p There is unfortunately no hint
